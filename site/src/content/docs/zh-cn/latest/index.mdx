---
title: Marivo
description: 面向 AI agents 的以指标为中心的分析运行时。
template: splash
hero:
  tagline: 面向 AI agents 的以指标为中心的分析运行时。
  image:
    alt: Marivo
    file: ../../../../assets/marivo-mark.svg
  actions:
    - text: 安装 Marivo
      link: /zh-cn/latest/installation/
      icon: right-arrow
    - text: 查看 GitHub
      link: https://github.com/chengxianglibra/marivo
      variant: secondary
---

import { Card, CardGrid } from '@astrojs/starlight/components';

Marivo 是一个 Python library，不是托管服务，也不是 chat UI。它把数据仓库整理成
AI agent 可以按契约分析的对象：用类型化的**语义层**固定每个 metric 和 dimension 的含义，
用类型化 operators 组织**以指标为中心的分析流程**，并为每个结论保留**可审计的证据链**。

Marivo 不是 text-to-SQL wrapper。Python declarations 是契约，ibis expressions 是执行语言，
typed frames 是分析步骤之间的边界。agent 组合的是经过声明的构件，而不是临时生成一段原始 SQL
再假设它正确。

<CardGrid>
  <Card title="语义层" icon="seti:data">
    Datasources、entities、metrics、dimensions 和 relationships 都用 Python 声明，并通过
    semantic ref 引用。分析面对的是名称和定义，而不是原始表。
  </Card>
  <Card title="以指标为中心的分析" icon="seti:python">
    Agents 从可信 metrics 出发，串联 typed intents：observe、compare、decompose、
    correlate、forecast 和质量检查。
  </Card>
  <Card title="证据链" icon="seti:config">
    Operators 记录 findings、propositions 和 assessments，结果可以追溯到产生它的输入。
  </Card>
  <Card title="就绪门禁" icon="seti:lock">
    就绪检查会在 authoring 和访问问题解决之前，阻止未完成的语义对象进入分析。
  </Card>
</CardGrid>

## 为什么 agent 分析需要 Marivo

如果直接把 agent 指向原始仓库并让它写 SQL，失败方式通常很明确：它可能臆造 join、漏掉必需的
过滤条件、误读含义不清的列，然后给出一个难以核验的答案。每次查询都从零开始，定义无法沉淀，
过程也难以复审。

Marivo 用一份 agent 必须遵循的契约，替代“生成 SQL 然后碰运气”的流程：

| 没有语义运行时 | 使用 Marivo |
| --- | --- |
| 每次查询都靠猜表名和列名 | 可信的 **semantic refs**（`sales.revenue`），携带人工编写的含义与 guardrails |
| 每次都重新推导定义 | 一份纳入版本控制的**声明式契约**，在 agents 与 sessions 之间共享 |
| 自由拼写的 SQL，错误往往只表现为错误的数字 | **类型化 intents 与 frames** —— 非法步骤会在任何 backend 操作之前明确报错 |
| 无法核验的结论 | 把每个结果链接回其输入的**证据链** |
| 半成品模型照样被分析 | 阻止未完成 semantics 进入分析的**就绪门禁** |

这样，agent 面对的是*含义*而不是列名；整个推理过程也可以被人类审阅、批准和重放。

## 它如何工作

1. **声明**：在 `models/datasources/` 中声明 datasources，在 `models/semantic/` 中声明
   semantics（domains、entities、dimensions、metrics）—— 使用 `marivo.datasource`
   （`md`）和 `marivo.semantic`（`ms`）。
2. **加载并检查就绪状态**：`ms.load()` 构建 catalog；`ms.readiness()` 会在 agent 所需对象
   完整且可达之前阻止分析。
3. **打开 session**：从一个 guiding question 出发，并从 catalog 解析出一个可信 metric。
4. **串联 typed intents** —— `observe → compare → decompose` 等 —— 每一步返回一个 typed
   frame，并写入 evidence。

## 第一次运行分析

```python
import marivo.analysis as mv

session = mv.session.get_or_create(name="revenue-check", question="Why did Q4 drop?")
catalog = session.catalog
revenue = catalog.get("sales.revenue")
region = catalog.get("sales.orders.region")

current = session.observe(revenue, timescope={"start": "2026-10-01", "end": "2027-01-01"}, grain="month", dimensions=[region])
baseline = session.observe(revenue, timescope={"start": "2025-10-01", "end": "2026-01-01"}, grain="month", dimensions=[region])

delta = session.compare(current, baseline)
attribution = session.decompose(delta, axis=region)
attribution.show()
```

## 后续步骤

<CardGrid>
  <Card title="安装" icon="rocket">
    [安装 Marivo](/zh-cn/latest/installation/)，并为你的 datasource 选择 backend extra。
  </Card>
  <Card title="快速开始" icon="open-book">
    [构建一个 project](/zh-cn/latest/quick-start/)：datasource、semantics，以及第一次
    以指标为中心的分析。
  </Card>
  <Card title="语义层" icon="seti:data">
    [声明含义](/zh-cn/latest/concepts/semantic-layer/)：每个对象与字段，及其用法。
  </Card>
  <Card title="分析流程" icon="seti:python">
    [运行分析](/zh-cn/latest/concepts/analysis-workflow/)：sessions、intents 和 typed frames。
  </Card>
</CardGrid>
