---
title: Readiness
description: The structural gate that keeps incomplete semantic objects out of analysis.
---

## What the user approves

The business owner approves the metric's definition and non-definition, row
population, filters, exclusions, refund and cancellation treatment, time axis, units,
additivity, valid breakdowns, limitations, owner, and intended use. These are business
decisions; passing a Marivo check cannot make or approve them.

## What readiness proves

Readiness is the zero-query technical gate between authoring and analysis. It consumes
fresh static verification and matching scoped runtime-preview evidence for the full
dependency closure and returns a `ReadinessReport`.

A new project can `ms.load()` successfully while readiness is still **blocked** —
loading only parses declarations. A ready report means the declarations and matching
preview evidence passed the technical handoff checks. It does not mean a business owner
approved the metric definition, filters, exclusions, units, or intended use.

## Copyable agent request

```text
Use the marivo-semantic skill to verify and preview this metric with the current bounded snapshot, then run readiness only for the metric and its dependency closure. Show me every blocker and warning, whether the verification and preview evidence is fresh and matches this scope, and the resulting readiness report. Treat ready only as technical handoff evidence: it is not business approval and does not give you permission to change the metric's meaning.
```

## How the agent hands off

The agent uses the same explicitly bounded snapshot gathered during authoring, repairs
one object at a time, and resolves every blocker before passing a ref to an analysis
session:

```python
import marivo.semantic as ms

catalog = ms.load()
revenue = catalog.get("metric.sales.revenue")
catalog.verify_object(revenue).show()
catalog.preview(revenue, using=snapshot).show()
report = catalog.readiness(refs=[revenue])
if report.status == "blocked":
    report.show()                        # bounded card: each blocker + its next step
    raise SystemExit("resolve readiness blockers before analysis")
```

The user reviews the proposed business contract alongside this report. Only after
business approval and a non-blocked scoped report does the agent hand the object to
`marivo-analysis`.

## Zero-query closeout after scoped runtime evidence

`catalog.readiness(refs=[...])` is a **pure in-memory check**. It never opens a datasource
connection — it reasons about the declarations themselves: missing definitions,
unknown refs, fan-out across datasources, fragile string references, and SQL
parity status. It does not inspect GitHub/GitLab approval state and does not
make audit-approval judgments.

Backend-touching validation is separate and runs on demand:

| Check | What it does | Touches backend? |
| --- | --- | --- |
| `catalog.verify_object(obj)` | Static object validation | No |
| `catalog.preview(obj, using=snapshot)` | Executes the scoped runtime smoke check and records metadata, never rows | Yes |
| `catalog.readiness(refs=[revenue])` | Consumes fresh verification/preview evidence | No |
| `ms.parity_check(ref)` | Optional, potentially unbounded metric/provenance SQL diagnostic | Yes |
| `ms.richness()` | Advisory coverage/depth report; never blocks | No |

:::caution[Readiness does not prove the query runs]
A `ready` report means the object passed static validation and has fresh matching
scoped preview evidence. It does not prove every future analysis query or full-source
truth. Parity is never required for readiness.
:::

## The report

`catalog.readiness(refs=[...])` returns a frozen `ReadinessReport`:

| Field | Type | Meaning |
| --- | --- | --- |
| `status` | `"ready" \| "ready_with_warnings" \| "blocked"` | Overall verdict (see below). |
| `blockers` | `tuple[ReadinessIssue, ...]` | Issues that must be fixed before handoff. |
| `warnings` | `tuple[ReadinessIssue, ...]` | Advisory issues that do not block. |
| `analysis_ready_refs` | `tuple[str, ...]` | Refs that passed — safe to hand to analysis. |
| `input_summary` | `ReadinessInputSummary` | What was checked: datasources, refs, and tables. |
| `checked_at` | `str` | ISO-8601 timestamp of the check. |

`status` is derived from severity: any blocker ⇒ `blocked`; warnings only ⇒
`ready_with_warnings`; otherwise `ready`.

Each `ReadinessIssue` carries the same teaching shape:

| Attribute | Meaning |
| --- | --- |
| `kind` | The issue kind (see table below). |
| `severity` | `"blocker"` or `"warning"`. |
| `refs` | The semantic refs the issue applies to. |
| `message` | What was expected and what was found. |
| `suggested_action` | The concrete next step. |

`report.show()` prints a bounded card; `report.to_dict()` returns the full machine
list for programmatic handling.

Optional enrichment gaps beyond `business_definition` and `guardrails` — such as
missing synonyms or examples — are reported by `ms.richness()` and do not change
readiness status.

## Issue kinds

| Kind | Severity | Trigger | Next step |
| --- | --- | --- | --- |
| `load_error` | blocker | A declaration failed to load. | Fix the semantic load errors and reload. |
| `unknown_ref` | blocker | A requested ref is not in the registry. | Fix the ref or remove it from the readiness scope. |
| `missing_business_definition` | blocker | A checked object has no `ai_context.business_definition`. | Add a `business_definition`. |
| `cross_datasource_unfederated` | blocker | A metric spans more than one datasource without federation. | Move integration upstream, enable a federated backend, or split the metric. |
| `missing_guardrails` | blocker (metric) / warning (other analyzable) | A checked metric has no `ai_context.guardrails` (blocker); entity / dimension / measure / time_dimension missing guardrails is a warning. | Add `guardrails`. |
| `sql_parity_unverified` | warning | A metric has `provenance` SQL but parity is unconfirmed. | Run `ms.parity_check(ref)`. |
| `fragile_string_ref` | warning | An object uses a fragile string reference. | Replace it with a stable object ref. |
| `time_dimension_pushdown_advisory` | warning | A time dimension may not push down to the partition. | Keep the raw partition column and declare its parse if the business axis matches it. |

## Scope the check

Pass typed catalog objects through `refs=` to scope readiness to exactly what an
analysis needs — Marivo resolves the full dependency closure (the
metric, its measures, dimensions, entities, and datasources) for you:

```python
revenue = catalog.get("metric.sales.revenue")
report = catalog.readiness(refs=[revenue])
for issue in report.blockers:
    print(issue.kind, issue.refs, "→", issue.suggested_action)
```

This is the required semantic gate before analysis. For how analysis records what it
concludes, see [Evidence](/en/latest/concepts/evidence/).
