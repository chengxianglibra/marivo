---
title: Installation
description: Prepare a Marivo project for an agent and choose a datasource backend.
---

Marivo requires Python 3.12 or newer.

## Prepare the project

Choose an empty or existing project directory. The recommended installer supports
macOS, Linux, and Windows Subsystem for Linux (WSL). It creates a virtual environment,
installs `marivo[all]`, and runs `marivo init` in that directory.

## Install Marivo

Run the recommended installer from the project directory:

```bash
curl -fsSL https://marivo.io/install.sh | bash
```

To manage the environment yourself, install the base library from PyPI:

```bash
pip install marivo
```

Install the backend extra that matches the datasource the project will query:

| Backend | Install command |
| --- | --- |
| DuckDB | `pip install "marivo[duckdb]"` |
| MySQL | `pip install "marivo[mysql]"` |
| Postgres | `pip install "marivo[postgres]"` |
| ClickHouse | `pip install "marivo[clickhouse]"` |
| Trino | `pip install "marivo[trino]"` |
| All packaged backends | `pip install "marivo[all]"` |

## Initialize the project

If you installed Marivo with pip, initialize the current directory before asking
an agent to work in it:

```bash
marivo --version
marivo init
```

`marivo init` is idempotent: it leaves existing files untouched and reports them as
skipped, so it is safe to re-run after an upgrade. Re-running adds only what is
missing. Use `marivo init --force` to delete the generated artifacts and recreate
them from scratch.

:::note
`init` refuses to silently skip a `marivo.toml` that contains invalid TOML. Fix or
remove it, or pass `--force`. Skill links are best-effort symlinks: if the platform
cannot create a symlink, `init` prints a warning and continues.
:::

## What the agent can use

| Path | Purpose |
| --- | --- |
| `marivo.toml` | Project manifest. The project name defaults to the directory name; local telemetry defaults to `enabled = "on"`. |
| `models/` | Datasource and semantic declarations. |
| `.marivo/` | Project-local sessions, evidence, and cached metadata. |
| `.agents/skills/` | Links to the `marivo-semantic` and `marivo-analysis` skills for compatible agents. |
| `.claude/skills/` | Links to the same skills for Claude Code. |
| `.codex/skills/` | Links to the same skills for Codex. |

```text
your-project/
  marivo.toml
  models/
  .marivo/
  .agents/skills/
  .claude/skills/
  .codex/skills/
```

## What to ask the agent to do next

After initialization, give the agent this bounded preparation request:

> Verify that this Marivo project is initialized correctly. Identify the datasource
> configuration and source tables that are still needed before semantic authoring.
> Stop before reading business data or defining any semantics. Report what is present,
> what is missing, and which decisions you need from me. For credentials, report only
> which approved credential fields need configuration, their `*_env` references and
> environment-variable names, and whether each variable is configured. Do not request,
> display, or commit credential values.

This separates project setup from access to business data and from decisions about
business meaning.

## What to review before continuing

Before moving to semantic authoring, confirm that:

- `marivo.toml`, `models/`, and `.marivo/` are present;
- the expected skill paths under `.agents/skills/`, `.claude/skills/`, and
  `.codex/skills/` are present, or the initialization warning explains why a symlink
  could not be created;
- the required datasource backend extra is installed; and
- credentials are supplied through approved environment-backed fields, not committed
  as plaintext in project files.

When those checks pass, continue to the [first semantic layer](/en/latest/quick-start/).
After its business meaning is approved and readiness is not blocked, continue to the
[first analysis](/en/latest/first-analysis/).
