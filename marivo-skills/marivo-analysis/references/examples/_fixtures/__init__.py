"""Shared fixtures for marivo-analysis examples.

Examples import this package via ``from _fixtures.tiny_semantic import ...``
because each example is run from the examples directory. The fixture imports
``marivo`` from the active Python environment.
"""
