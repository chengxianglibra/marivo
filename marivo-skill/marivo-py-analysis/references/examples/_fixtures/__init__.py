"""Shared fixtures for marivo-py-analysis examples.

Examples import this package via ``from _fixtures.tiny_semantic import ...``
because each example is run with cwd=<examples dir>, which puts this folder
on ``sys.path`` automatically.
"""
