"""Private datasource-to-semantic authoring domain kernel.

This package owns shared mechanical authoring values. Public authoring APIs,
registry meaning, contract construction, and rendering adapters remain owned by
``marivo.datasource`` and ``marivo.semantic``.
"""
